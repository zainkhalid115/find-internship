<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Internships - Find Internship</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .internship-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        .internship-card {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            text-align: left;
            border-left: 5px solid #007bff;
        }
        .internship-card h3 {
            margin-top: 0;
            color: #333;
        }
        .internship-card p {
            font-size: 0.9em;
            color: #666;
            margin-bottom: 5px;
        }
        .internship-card .details-link {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 8px 15px;
            border-radius: 4px;
            text-decoration: none;
            margin-top: 10px;
        }
        .internship-card .details-link:hover {
            background-color: #0056b3;
        }
        .add-internship-link {
            display: inline-block;
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            border-radius: 4px;
            text-decoration: none;
            margin-top: 20px;
        }
        .add-internship-link:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>All Available Internships</h2>

        <!-- Display flash messages -->
        <% if (success_msg) { %>
            <div class="alert alert-success"><%= success_msg %></div>
        <% } %>
        <% if (error_msg) { %>
            <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>
        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <a href="/internships/add" class="add-internship-link">Post a New Internship</a>

        <% if (internships.length > 0) { %>
            <div class="internship-list">
                <% internships.forEach(function(internship) { %>
                    <div class="internship-card">
                        <h3><%= internship.title %></h3>
                        <p><strong>Company:</strong> <%= internship.company %></p>
                        <p><strong>Location:</strong> <%= internship.location %></p>
                        <p><strong>Posted By:</strong> <%= internship.postedBy ? internship.postedBy.username : 'Anonymous' %></p>
                        <p><strong>Posted On:</strong> <%= internship.datePosted.toLocaleDateString() %></p>
                        <a href="/internships/<%= internship._id %>" class="details-link">View Details</a>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No internships available at the moment. Be the first to post one!</p>
        <% } %>
        <p><a href="/dashboard">Back to Dashboard</a> | <a href="/">Back to Welcome</a></p>
    </div>
</body>
</html>